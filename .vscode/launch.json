{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "build-script",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}/build.ps1",
            "cwd": "${cwd}"
        }
    ]
}